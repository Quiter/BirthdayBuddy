package com.heckmannch.birthdaybuddy.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Predefined color presets for the calendar event color picker.
 * Ordered by visual harmony: warm → cool → neutral.
 */
val CalendarPresetColors: List<Color> = listOf(
    Color(0xFFE91E63), // Pink
    Color(0xFF9C27B0), // Purple
    Color(0xFF2196F3), // Blue
    Color(0xFF00BCD4), // Cyan
    Color(0xFFFFC107), // Amber
    Color(0xFFFF9800), // Orange
    Color(0xFF795548), // Brown
)

/**
 * Predefined accent seed colors for the app theme color picker.
 * Each color is a seed for Material You HCT color scheme generation.
 */
val AccentPresetColors: List<Color> = listOf(
    Color(0xFF005FAF), // Blue
    Color(0xFF388E3C), // Green
    Color(0xFFBA1A1A), // Red
    Color(0xFFF57C00), // Orange
    Color(0xFFC2185B), // Pink
    Color(0xFF6750A4), // Purple (Default)
)
